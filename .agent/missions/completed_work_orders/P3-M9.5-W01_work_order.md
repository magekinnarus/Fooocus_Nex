# Work Order: P3-M9.5-W01 — Extract Float Precision Utilities
**ID:** P3-M9.5-W01
**Mission:** P3-M9.5
**Status:** Completed
**Depends On:** —

## Mandatory Reading
- `.agent/rules/02_Documentation_and_Mission_Standards.md`
- `.agent/summaries/04_Inference_Architectural_Guideline.md`
- `.agent/summaries/05_Local_Environment_Guidelines.md`
- `Fooocus_Nex/ldm_patched/modules/float.py` (source — 68 lines)
- `Fooocus_Nex/backend/weight_ops.py` (consumer — lines 8, 224)
- `Fooocus_Nex/backend/patching.py` (consumer — lines 13, 336)

## Objective
Extract the 3 functions from `ldm_patched/modules/float.py` into a new `backend/float_ops.py` and update all consumers. Also inline the `UnetWrapperFunction` type alias from `comfy_types`.

## Scope

### Create `backend/float_ops.py` [NEW]
Copy these functions verbatim from `ldm_patched/modules/float.py`:
- `calc_mantissa()` (lines 3–11)
- `manual_stochastic_round_to_float8()` (lines 14–46)
- `stochastic_rounding()` (lines 50–67)

Only dependency: `import torch`. No other imports needed.

### Update Consumers
| File | Current Import | Change To |
|------|---------------|-----------|
| `weight_ops.py` | `import ldm_patched.modules.float` | `from . import float_ops` |
| `weight_ops.py` line 224 | `ldm_patched.modules.float.stochastic_rounding(...)` | `float_ops.stochastic_rounding(...)` |
| `patching.py` | `import ldm_patched.modules.float` | `from . import float_ops` |
| `patching.py` line 336 | `ldm_patched.modules.float.stochastic_rounding(...)` | `float_ops.stochastic_rounding(...)` |

### Inline `comfy_types` (Trivial)
| File | Current | Change To |
|------|---------|-----------|
| `patching.py` line 16 | `from ldm_patched.modules.comfy_types import UnetWrapperFunction` | `from typing import Callable`<br>Define `UnetWrapperFunction = Callable` locally or use `Callable` directly |

## Verification
1. Run `app.py` with `test_sd15_quality_config.json` — generation succeeds, output image unchanged
2. Run `app.py` with `test_sdxl_quality_config.json` (if GGUF available locally) — generation succeeds
3. Confirm zero `ldm_patched.modules.float` imports: `grep -r "ldm_patched.modules.float" backend/`
4. Confirm zero `comfy_types` imports: `grep -r "comfy_types" backend/`

## Success Criteria
- `backend/float_ops.py` exists with 3 functions
- Zero `ldm_patched.modules.float` imports in backend
- Zero `ldm_patched.modules.comfy_types` imports in backend
- SD1.5 generation identical to pre-extraction baseline
